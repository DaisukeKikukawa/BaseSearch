<h1>チームカテゴリー詳細</h1>

<div class="row">
  <div class="card mt-4 mb-4 pt-4" style="width: 40rem;">
    <div class="card-header bg-secondary">
      カテゴリー名
    </div>
    <div class="card-block">
      <p class="card-text p-4"><%= @team_category.name %></p>
    </div>
  </div>
</div>

<div class="btn-group" role="group" aria-label="Basic example">
  <%= link_to("戻る", admin_team_categories_path, class: "btn btn-secondary rounded") %>
  <%= link_to("編集", edit_admin_team_category_path(@team_category), class: "btn btn-primary mx-1 rounded") %>
  <%= link_to('削除', [:admin, @team_category], method: :delete, data: { turbo_method: :delete, turbo_confirm: "削除しますか？" }, class: "btn btn-danger rounded") %>
</div>
